# Local overrides (machine-specific; never commit)
home/.bashrc.local
*.local

# OS / editor junk
.DS_Store

# Tool caches
.cache/

# Secrets
home/.ssh/

# Cursor project dir (repo root only; home/.cursor/ stays versioned)
/.cursor/

# ui-ux-pro-max search helpers — keep out of the repo (language stats + noise)
home/.cursor/skills/ui-ux-pro-max/scripts/
home/.cursor/skills/ui-ux-pro-max/**/*.py
home/.cursor/skills/ui-ux-pro-max/**/__pycache__/

# Graphify — generated output and local-only tooling
graphify-out/
raw/
.graphifyignore
scripts/export-github-corpus.sh
