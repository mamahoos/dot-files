# Local secret gate — same engine as .github/workflows/gitleaks.yml (CI).
# Install once after clone:  pre-commit install
# Skip one commit:           SKIP=gitleaks git commit ...
repos:
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.30.1
    hooks:
      - id: gitleaks
